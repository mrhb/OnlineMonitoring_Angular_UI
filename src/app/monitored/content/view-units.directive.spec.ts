import { ViewUnitsDirective } from './view-units.directive';

describe('ViewUnitsDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewUnitsDirective();
    expect(directive).toBeTruthy();
  });
});
