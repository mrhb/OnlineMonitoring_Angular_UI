import { TrendsViewDirective } from './trends-view.directive';

describe('TrendsViewDirective', () => {
  it('should create an instance', () => {
    const directive = new TrendsViewDirective();
    expect(directive).toBeTruthy();
  });
});
